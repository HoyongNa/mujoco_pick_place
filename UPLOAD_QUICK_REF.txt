╔═══════════════════════════════════════════════════════════════╗
║           GITHUB UPLOAD - QUICK REFERENCE                     ║
╚═══════════════════════════════════════════════════════════════╝

✅ YOUR .GITIGNORE ALREADY EXCLUDES ROBOCASA!

═══════════════════════════════════════════════════════════════

🚀 QUICK UPLOAD (3 Commands)

cd ~/mujoco_pick_place

# 1. Remove robocasa from tracking (if needed)
git rm -r --cached robocasa/ 2>/dev/null || echo "Already excluded"

# 2. Commit your changes
git add .
git commit -m "Add RoboCasa installation docs and helper scripts"

# 3. Push to GitHub
git push origin main

═══════════════════════════════════════════════════════════════

🤖 AUTOMATED SCRIPT

# Use the helper script to do everything
bash upload_to_github.sh

This script will:
  ✓ Check if robocasa is tracked
  ✓ Remove it if needed
  ✓ Verify .gitignore
  ✓ Check for large files
  ✓ Offer to commit and push

═══════════════════════════════════════════════════════════════

📋 FIRST TIME SETUP

If you haven't created a GitHub repository yet:

1. Go to https://github.com/new
2. Create repository: mujoco_pick_place
3. Don't initialize with README (you have one)
4. Copy the repository URL

Then run:
  cd ~/mujoco_pick_place
  git remote add origin https://github.com/YOUR_USERNAME/mujoco_pick_place.git
  git branch -M main
  git push -u origin main

═══════════════════════════════════════════════════════════════

🔍 VERIFICATION COMMANDS

# Check what's tracked
git ls-files | grep robocasa
  → Should return nothing

# Check .gitignore is working
git check-ignore robocasa/
  → Should return: robocasa/

# See what will be uploaded
git status
git ls-files

# Check repository size
git count-objects -vH

═══════════════════════════════════════════════════════════════

📦 WHAT GETS UPLOADED

✅ INCLUDED:
  - All Python source code
  - Documentation (README.md, ROBOCASA_INSTALL.md, etc.)
  - Helper scripts (fix_robocasa_deps.py, etc.)
  - Robot models (XML files)
  - Configuration files

❌ EXCLUDED (in .gitignore):
  - robocasa/ directory
  - robosuite/ directory
  - __pycache__/
  - *.pyc
  - Build artifacts (*.so, *.dll)
  - Large data files (*.h5, *.pkl)
  - API keys

═══════════════════════════════════════════════════════════════

👥 WHAT USERS WILL DO

After cloning your repository:

git clone https://github.com/YOUR_USERNAME/mujoco_pick_place.git
cd mujoco_pick_place

# Install basic dependencies
pip install -r requirements.txt

# Clone and install RoboCasa (following your docs)
git clone https://github.com/robocasa/robocasa.git
pip install robosuite --break-system-packages
cd robocasa && pip install -e . --break-system-packages

# Run simulation
python3 main.py

Your documentation explains all of this! ✅

═══════════════════════════════════════════════════════════════

📊 REPOSITORY SIZE

Without robocasa: ~5-50 MB   ← Your setup ✅
With robocasa:    ~500 MB+   ← Too large! ❌

Excluding robocasa makes your repo 10-100x smaller!

═══════════════════════════════════════════════════════════════

🆘 TROUBLESHOOTING

Problem: "robocasa still showing in git status"
Fix: git rm -rf --cached robocasa/

Problem: "Repository too large"
Fix: Check for large files with:
     git ls-files | xargs ls -lh | sort -k5 -h -r | head -20

Problem: "Can't push to GitHub"
Fix: Check remote URL:
     git remote -v
     git remote set-url origin https://github.com/YOUR_USERNAME/mujoco_pick_place.git

═══════════════════════════════════════════════════════════════

📚 DETAILED DOCUMENTATION

For complete guide, see:
  → GITHUB_UPLOAD_GUIDE.md    (Full instructions)
  → upload_to_github.sh       (Automated helper)
  → ROBOCASA_INSTALL.md       (User installation guide)

═══════════════════════════════════════════════════════════════

🎯 READY TO UPLOAD?

Option 1 - Automated:
  bash upload_to_github.sh

Option 2 - Manual:
  git add .
  git commit -m "Your message"
  git push origin main

═══════════════════════════════════════════════════════════════
